const config = {
  geth: {
    host: {
      http: "http://localhost:8545",
      websocket: "ws://localhost:8546"
    },
    explorer: {
      host: "localhost",
      port: 3001
    }
  }
};

export default config;